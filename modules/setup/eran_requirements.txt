# Core dependencies - REQUIRED (with ERAN-specific versions)
numpy==1.23.5
tensorflow==2.9.3
onnx>=1.9.0
protobuf==3.19.6
torch
torchvision
mpmath

# ERAN-specific - REQUIRED
pycddlib

# ONNX ecosystem - REQUIRED
onnxruntime
onnxoptimizer>=0.3
skl2onnx>=1.14
git+https://github.com/Verified-Intelligence/onnx2pytorch.git
onnx2torch

# Utilities - REQUIRED
tqdm
pyyaml>=6.0
psutil>=5.9.5

# Note: The following are transitive dependencies (auto-installed):
# - pillow (via torch/torchvision) - ERAN pins pillow>=8.1.0, but torch handles this
# - scipy, requests (via torch/torchvision)
# - typing-extensions (via onnx/tensorflow)
# - All nvidia-* packages (via torch with CUDA)
# - sympy, networkx (via torch)
# - certifi, charset-normalizer, urllib3, idna (via requests)
# - markupsafe, jinja2 (via tensorflow)
# - Other utility packages
